---
scope: EkoDoc v1 - architecture complete (initiative)
purpose: build-substrate
altitude: initiative
updated: 2026-09-09T15:08
---

- (decision) Coaching path choisi ; livrable = spine seul (pas de support de discussion additionnel)
- (event) Greenfield confirme : aucun scaffold Laravel existant (pas de composer.json/artisan/app) dans le repo
- (constraint) Herite du PRD (NFR1) : Laravel via Herd, local, pas d'infra serveur partagee en v1
- (constraint) Herite du PRD (NFR3) : pas d'authentification/roles en v1, mono-utilisateur
- (constraint) Herite de EXPERIENCE.md : Tailwind CSS sans librairie de composants tierce, pas de framework frontend impose
- (version) PHP 8.5 ; Laravel 13 (releve 2026-03, exige PHP 8.3 minimum, compatible) ; Tailwind CSS 4 ; Inertia.js ; Vite ; MySQL ; Herd (edition gratuite) - annonce par l'utilisateur, verifie compatible PHP/Laravel via recherche web
- (version) WYSIWYG editeur : TipTap (@tiptap/vue-3, v3.x, MIT, actif) - verifie via recherche web, integration Vue 3 officielle, extensions Table/Image disponibles pour FR8/FR9
- (decision) Paradigme : Thin Controller -> Action (invokable PHP pur, __invoke, sans package) -> DTO (readonly PHP natif) -> Eloquent Model. Convention transverse a tous les projets de l'utilisateur, pas specifique a EkoDoc.
- (decision) Clean Code / SOLID obligatoire : pas de valeurs magiques, pas de commentaires sauf complexite technique/metier reelle, nommage explicite en anglais uniquement, 100% couverture de tests
- (decision) Tests : Pest (defaut moderne Laravel 12/13), cible 100% de couverture
- (decision) Classement (FR2) : categories a plat (table categories, un document = zero ou une categorie), pas de hierarchie de dossiers - coherent avec le selecteur unique deja specifie en UX
- (decision) Import Google Docs/Sheets : passe par export manuel de l'utilisateur (.docx/.xlsx/PDF) puis pipeline d'import classique - pas d'integration API/OAuth Google Drive, coherent avec NFR1 (pas de dependance externe partagee)
- (question) Formats herites .doc/.xls : statut inconnu (l'utilisateur n'est pas certain du volume ni du format des documents existants) - reste une question ouverte non bloquante, formats cibles restent PDF/.docx/.xlsx (NFR4) ; a etendre si besoin identifie en cours de route
- (version) Export PDF (FR11/NFR5) : spatie/browsershot v5.4 (verifie web, actif) - rendu HTML/CSS via Chromium headless reel, fidelite image garantie car identique au rendu ecran
- (version) Export Word (FR12) : phpoffice/phpword v1.4.0 (verifie web, actif, LGPL) via import HTML - fidelite position image NON garantie au meme niveau que le PDF (mapping HTML->OOXML), documente comme risque assume plutot que resolu, coherent avec le cas d'echec deja anticipe en EXPERIENCE.md Flow 3
- (decision) Recherche fulltexte (FR6/NFR2) : Laravel Scout + driver database (MySQL FULLTEXT en interne), pas de moteur externe (Meilisearch ecarte, overkill pour ~350 documents)
- (decision) Previsualisation Word/Excel (FR4) : LibreOffice headless invoque en ligne de commande depuis une Action Laravel, resultat PDF mis en cache sur disque - pas de Gotenberg/Docker, coherent avec usage solo local (NFR1)
- (version) Extraction de texte a l'import pour indexation fulltexte : smalot/pdfparser (PDF, maintenance limitee mais acceptable - echec non bloquant, document reste importe/consultable, juste absent de la recherche) ; phpword/phpspreadsheet en lecture pour Word/Excel (reutilisation des libs deja retenues pour l'export)
- (decision) Stockage fichiers originaux : disque local Laravel prive (storage/app/private), jamais public - servis via route de telechargement controlee, prepare l'ouverture multi-utilisateurs post-v1
- (decision) Traitement a l'import : synchrone (extraction texte + indexation avant redirection vers Fiche document), pas de queue/worker - coherent usage solo, fichiers de taille modeste
- (decision) Modele de donnees : table unique documents avec discriminant source (imported|created), table categories a plat, pas de table users (NFR3 pas d'auth) - unifie la Bibliotheque comme deja specifie en UX, pas de sur-ingenierie
- (decision) Routage : pages Inertia (GET) + Actions invoquees depuis des controllers fins pour les mutations (POST/PUT/DELETE) - pas de couche API JSON separee, l'app n'expose pas d'API publique en v1
- (question) Sauvegarde/backup des donnees locales (documents + DB MySQL) : aucune strategie definie en v1, pousse en Deferred - risque accepte pour un usage solo, a revisiter si le corpus devient critique ou avant l'ouverture multi-utilisateurs
- (event) spine draft ecrit (ARCHITECTURE-SPINE.md)
- (decision) AD-12 (export Word) confirme best-effort documente ; restreint explicitement la garantie NFR5 'critique' au format PDF (AD-11), le Word reste soumis a NFR5 en intention (jamais d'echec silencieux) mais pas en garantie de resultat
- (change) Reviewer Gate applique : AD-14 (images editeur, fichier+route) ajoute - gap critique convergent (rubrique+PRD+UX) ; AD-15 (suppression definitive + nettoyage complet) ajoute ; AD-16 (category_id un seul point d'ecriture + DeleteCategoryAction) ajoute
- (change) AD-6 corrige : ordre extraction->creation Document, indexation Scout automatique via evenement saved (pas d'appel manuel avant creation, contradiction adversariale F4 corrigee)
- (change) AD-8 etendu : unifie recherche et filtres sous un seul chemin de requete (adversarial F13) ; AD-9 etendu : extracted_text derive de content_html pour source=created (gap UX), OCR explicitement absent/documente en v1 (gap addendum)
- (change) AD-10 corrige : cache invalide seulement par suppression/re-import (pas de scenario fichier modifie en place) ; PDF.js explicitement ecarte au profit du lecteur PDF natif du navigateur (question ouverte de l'addendum resolue)
- (change) AD-5 : ajout d'une phrase explicite labellisant le choix categories-a-plat comme resolution deliberee de l'ambiguite FR2 (dossiers et/ou categories), pas un oubli
- (change) AD-7 etendu : detection fichier manquant/illisible assignee explicitement a la route de telechargement/preview ; Consistency Conventions clarifie que 100% coverage est verifie manuellement (pas de CI en v1) ; Deferred : OCR et empreinte Chromium/LibreOffice ajoutes comme compromis assumes documentes ; Structural Seed et Capability Map mis a jour (UploadEditorImageAction, DeleteCategoryAction, colonnes Document/Category, FR9, FR4 cree, suppression)
- (event) Reviewer Gate resolu : 6 revues (rubrique, adversariale x14 findings, versions techniques, reconciliation PRD/brief/UX) - tous les gaps convergents et mecaniques corriges, un seul choix produit tranche avec l'utilisateur (fidelite export Word)
- (version) Correction post-verification : Inertia.js reel actuel = v3.0 (pas 2.x, assertion initiale non verifiee) - rupture majeure vs 2.x (suppression Axios, ESM uniquement, API renommees) ; Vite reel actuel = v8.x (pas 5.x/6.x). Pest confirme v5.x (exige PHP 8.4+, compatible). phpoffice/phpspreadsheet confirme compatible. smalot/pdfparser corrige a 2.12.5 (etait 2.12.4).
- (change) Polish applique (bmad-review lenses=structure,prose) : legende de schema AD ajoutee, Capability Map deplacee juste apres Invariants, liste d'Actions dedupliquee dans Structural Seed ; 9 corrections prose (accord des verbes, terminologie builder/constructeur unifiee, streame, reference il->le document, sujets manquants, citation section Flow 3, calque en place retire)
- (event) spine finalized
- (decision) AD-6 amended (2026-09-01): text extraction moved from synchronous in-request processing to a queued job (ExtractDocumentTextJob, database driver) after a real 135-page PDF exceeded every reasonable HTTP/proxy timeout during Story 1.1 testing. Import (storage + Document row) stays synchronous and transactional; extraction now runs out-of-band via a worker (queue:work/queue:listen), tracked by a new extraction_status enum (pending/processing/completed/failed) on documents. AD-9 rule updated to match. Trade-off accepted: requires a queue worker process running, which the app did not need before.
- (constraint) Sprint Change Proposal 2026-09-09 (Epic 3 consolidation v1.1) approuve : categorie supprimee sans migration au profit de tags illimites ; necessite amendement AD-5, retrait AD-16, ajout AD-17 (pieces jointes) et AD-18 (page Configuration tags).
- (decision) AD-5 amende (tags illimites remplace categorie unique) : table 'tags' (id, name) a plat sans hierarchie ; relation many-to-many via pivot 'document_tag' (document_id, tag_id) ; pas de limite de tags par document ; tags geres exclusivement depuis la page Configuration (FR14), jamais de creation a la volee ailleurs. Remplace AD-5 v1 (categorie unique) et retire AD-16. Table 'categories', colonne 'documents.category_id', 'CategorizeDocumentAction', modele/controller 'Category' supprimes (pas depreciees) - aucune migration des donnees existantes (decision explicite utilisateur 2026-09-09).
- (change) AD-16 retire ('category_id : un seul point d ecriture') - obsolete, category_id et CategorizeDocumentAction supprimes par l amendement AD-5.
- (decision) AD-17 ajoute - Pieces jointes sur document cree (FR13) : nouvelle table 'document_attachments' (id, document_id, file_path, original_filename, mime_type, timestamps) ; fichiers sur storage/app/private/documents/{document_id}/attachments/{uuid}.{ext}, jamais public (meme discipline qu AD-7) ; AttachDocumentFileAction/DetachDocumentFileAction comme points d entree uniques (AD-2) ; DeleteDocumentAction (AD-15) etendue pour nettoyer aussi ces fichiers, dans le meme ordre.
- (decision) AD-18 ajoute - Page Configuration : gestion des tags (FR14) : TagController expose une page Inertia listant les tags avec create/rename/delete ; DeleteTagAction detache le tag de tous les documents (delete des lignes du pivot) sans jamais supprimer les documents eux-memes - meme discipline que l ancien DeleteCategoryAction (retire).
- (decision) Capability -> Architecture Map mise a jour : retrait des lignes categorie (FR2/FR10) ; ajout FR2/FR7/FR10 (Tags, AD-5), FR13 (Pieces jointes, AD-17), FR14 (Configuration, AD-18).
- (decision) Structural Seed mis a jour : retrait de Category.php, CategoryController.php, Actions/Category/, CategoryData.php, CategorySelector.vue ; ajout de Tag.php, TagController.php, Actions/Tag/ (CreateTagAction/RenameTagAction/DeleteTagAction), TagData.php, AttachDocumentFileAction/DetachDocumentFileAction, tables document_tag (pivot) et document_attachments, dossier storage documents/{document_id}/attachments/{uuid}.{ext}, composant TagSelector.vue, page Configuration.vue.
- (event) Migration DB actee : DROP TABLE categories, DROP COLUMN documents.category_id - pas de retromigration de donnees (decision explicite 2026-09-09).
- (change) Reconciliation PRD (bmad-architecture, subagent) : gap trouve - FR6/FR13 exigent l indexation fulltexte du contenu de chaque piece jointe et un telechargement/apercu individuel ; AD-17 v1 (draft) ne couvrait que le stockage prive, pas l extraction/recherche ni la route de service. AD-17 etendue : bind FR6 en plus de FR13 ; table document_attachments recoit extracted_text/extraction_status (memes colonnes qu AD-9) ; ExtractDocumentTextJob (AD-6) generalise pour cibler Document ou DocumentAttachment au lieu d une logique d extraction dupliquee ; Document::toSearchableArray() agrege le texte de ses pieces jointes completees (pas de ligne Scout separee, coherent AD-8) ; DocumentAttachmentController@download/@preview ajoute avec la meme discipline que AD-7 (verification existence/lisibilite, route applicative, jamais de lien direct).
- (change) Reviewer Gate (adversarial, subagent) : 3 gaps corriges avant finalisation - (1) AD-5 etendue : SyncDocumentTagsAction unique point d ecriture du pivot document_tag (sync() complet, jamais attach/detach incremental), TagSelector.vue explicitement non-creatable et partage entre tous les ecrans ; (2) AD-15 Rule reecrite pour nommer explicitement le nettoyage des fichiers document_attachments (jamais une simple cascadeOnDelete DB) ; (3) Capability Map corrigee : FR7 ne vit plus que sous AD-8 (filtre tag = meme chemin de requete que la recherche, plus de duplication avec AD-5), FR3 references AD-4+AD-5, FR13 gagne son Form Request/DTO dans le Structural Seed. AD-17 etend aussi ses types de fichiers acceptes (memes que FR1).
- (event) Spine amende et reconcilie (2026-09-09) : AD-5 amendee, AD-16 retiree, AD-17/AD-18 ajoutees, Capability Map + Structural Seed + Deferred mis a jour. Reviewer Gate complet (rubrique, adversariale, versions techniques, reconciliation PRD) - tous les gaps convergents corriges avant clotures. Reste hors perimetre de cette session : revision UX (DESIGN.md/EXPERIENCE.md), decoupage Epic 3.
- (change) Structural Seed: DocumentCard.vue renomme en DocumentRow.vue - alignement avec DESIGN.md.Components.document-row (session bmad-ux du 2026-09-09, listing Bibliotheque en table au lieu de cartes).
